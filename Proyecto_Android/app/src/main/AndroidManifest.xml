<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Clases.Favoritos"></activity>
        <activity android:name=".Clases.CancionPantallaCompleta" />
        <activity android:name=".Base_Datos.Registro$Usuario" />
        <activity android:name=".Base_Datos.Registro" />
        <activity android:name=".Clases.ClasePrincipal" />
        <activity android:name=".Clases.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>